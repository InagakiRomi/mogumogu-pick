CREATE TABLE IF NOT EXISTS restaurant_categories (
    category_id      INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1) PRIMARY KEY,
    group_id         INT NOT NULL,
    display_order    INT NOT NULL,
    category_name    VARCHAR(32) NOT NULL,
    CONSTRAINT uk_restaurant_categories_group_name UNIQUE (group_id, category_name)
);

CREATE TABLE IF NOT EXISTS restaurant (
    restaurant_id      INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1) PRIMARY KEY,
    group_id           INT NOT NULL,
    category_id        INT NOT NULL,
    display_order      INT NOT NULL,
    restaurant_name    VARCHAR(64) NOT NULL,
    image_url          VARCHAR(512),
    selected_count     INT NOT NULL DEFAULT 0,
    last_selected_at   TIMESTAMP,
    created_at         TIMESTAMP,
    updated_at         TIMESTAMP,
    note               VARCHAR(512),
    CONSTRAINT fk_restaurants_category
        FOREIGN KEY (category_id) REFERENCES restaurant_categories (category_id)
);
